# 计划事件与任务视图

## 当前状态
结构化计划由 `plan.create`、`plan.update` 和 `task.*` 事件驱动，状态保存在 `state.plan`、`planRuntimeByTaskId`、`taskItemsById` 等字段。浮动计划面板由 `PlanPanel` 渲染。

## 核心职责
- 将 plan create/update 归一为 `Plan`。
- 将 task start/complete/fail/cancel 更新为 task runtime 和 task meta。
- 当 run 已进入 complete/cancel/error 终态但仍有同一 run 的活动任务时，前端将这些残留任务同步收敛为对应终态，避免计划圆点继续显示运行中动画。
- 在计划面板展示总进度、每个任务状态和运行耗时。
- 将 timeline node 与 taskId/taskGroupId/subAgentKey 绑定，便于按任务观察运行过程。

## 核心流程
`planEventProcessor.ts` 处理 `plan.create/update`，`taskEventProcessor.ts` 处理 `task.*`，并分别生成计划与任务命令。`PlanPanel` 读取全局状态，按任务状态计算进度、状态点和耗时，并提供展开/收起交互。

## 边界与非目标
- PlanPanel 不是 HITL plan decision 弹窗；后者属于 awaiting plan。
- 计划视图只展示后端事件，不在前端生成或修改 plan。
- 任务耗时基于前端收到的时间戳计算，仅用于观察。

## 相关文件
- `../src/features/events/lib/processors/planEventProcessor.ts`
- `../src/features/events/lib/processors/taskEventProcessor.ts`
- `../src/features/events/lib/processors/eventProcessorShared.ts`
- `../src/features/plan/components/PlanPanel.tsx`
- `../src/app/state/toolTypes.ts`
- `../src/app/state/reducerPlan.ts`
- `../src/app/state/reducerTasks.ts`
- `../src/features/plan/components/PlanPanel.test.ts`
