# 运行时配置与功能开关

## 当前状态
运行时配置由开发服务或 Desktop 托管层写入 `runtime-config.js`，浏览器端通过 `globalThis.__AGENT_WEBCLIENT_RUNTIME_CONFIG__` 读取。`BACKEND_MODE` 固定选择 `platform` Token 模式或 `gateway` Session Cookie 模式，未配置时默认为 `platform`。功能开关集中在 `src/shared/config/featureFlags.ts`，语音是否启用由 `VOICE_BASE_URL` 推导出的 `VOICE_ENABLED` 控制。配置不会进入浏览器 bundle。公开分享不经过 WebClient，因此不声明分享页面运行时配置。

## 核心职责
- 读取 `.env` 或部署环境变量中的前端开关。
- 暴露 debug panel、delta logs、settings menu、quick actions、memory、voice 等开关。
- 在开发服务和 Desktop 宿主中生成浏览器可读取的 runtime config。
- 避免把私有上游地址或 token 硬编码进业务代码。

## 核心流程
开发模式下，Webpack dev server 的 `/runtime-config.js` 动态返回配置脚本。Program Bundle 运行时由 Desktop main process 根据 manifest 与宿主参数返回同名脚本。业务代码通过 `readRuntimeConfigValue` 和 feature flag helper 读取开关，再决定是否展示对应入口。

## 边界与非目标
- `.env.example` 是变量契约；真实 `.env` 不提交。
- 配置只控制前端入口与客户端行为，不替代后端能力探测。
- 不在 React 组件中直接读取 `process.env`。

## 相关文件
- `../.env.example`
- `../webpack.config.js`
- `../scripts/release-assets/program/manifest.template.json`
- `../src/shared/config/runtimeConfig.ts`
- `../src/shared/config/backendMode.ts`
- `../src/shared/config/featureFlags.ts`
- `../src/shared/config/featureFlags.test.ts`
