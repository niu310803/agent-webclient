# 手工测试用例

## 当前状态
本项目以 Jest 单元测试为主，核心联调依赖外部 AGENT API、语音服务、Desktop 宿主和浏览器能力。手工测试用于覆盖代理、实时流、HITL、附件、Viewport、语音和发布包结构等跨系统场景。

## 核心职责
- 给出本地开发、容器部署和 release 包的最小检查路径。
- 覆盖 SSE/WS、attach、submit、steer、interrupt 等运行控制。
- 覆盖附件上传、artifact resource、viewport iframe、HITL 四种 mode。
- 明确哪些失败应回到后端协议或部署配置排查。

## 核心流程
先准备 `.env`，显式设置 `DESKTOP_APP=false` 并启动 `make dev` 或容器。确认页面可打开、agents/chats 按当前 backend 能力表通过主 WebSocket 加载，再执行一次普通 query、一次运行中 steer、一次 interrupt、一次带附件 query。随后用后端可触发的样例验证 artifact.publish、viewport block、frontend tool、question、approval、form、plan awaiting 和 voice。

## 双环境与独立 Surface 回归

1. Standalone 打开 `/`，确认左右侧栏和 TerminalDock 均存在；Platform 下 agents/chats 与 Run/Push/Terminal 共用一条 `/ws`，project/resource 继续走 HTTP。
2. 分别在 `DESKTOP_APP=false` 与 Frame Port Desktop 环境打开 `/agent/:agentKey`。Desktop Agent TopNav 右侧快捷组为空，Desktop 外层仅在 Main Chat 提供 WorkPanel 按钮；管理页、Copilot、Website/WebApp 不出现该入口。Standalone Agent/root 原有按钮保持不变。打开 `/copilot/:agentKey`，确认 BTW 与 Terminal 不可用。
3. 在 `DESKTOP_APP=false` 的根页面创建稳定 Chat 后，通过 Platform 触发七个 `desktop.workpanel.*`：确认内置项 ID 为 `sidebar:*`、网页项 ID 稳定为 `web:<base64url>`，refresh 会激活目标，closeWorkpanel 隐藏但不删除网页；错误 Chat、native、其他 module、固定/不可关闭网页均拒绝。再在 `DESKTOP_APP=true` 确认 guest 未注册 `desktop.action.call` provider。
4. 直接刷新 `/overview/:chatId`、`/debug/:chatId`、`/planning-viewer/:planningId?chatId=...`、`/source-viewer/:sourceId?chatId=...&chunkId=...`、`/resource-viewer/:agentKey?chatId=...&file=...`、`/file-viewer/:agentKey?path=...&line=...`、`/skill-viewer/:key`、`/terminal/:agentKey?terminalKey=main` 与 `/project/:agentKey`。缺少或错误稳定 ID 必须显示无效目标；File 不请求 Chat。旧 `/artifact-view/:agentKey`、`/reference-view/:agentKey`、query `agentKey`、`history=1`、`/overview?view=...` 均不得解析、重定向或输出。
   - 单独验证 `/history` 默认 All，`/history?agentKey=<有效 Agent>` 初始化对应筛选且可切换，未知 Agent 在 Agents 与 Chats 都加载完成后回退 All。Desktop Host 中选择记录发送 history-open-chat 请求且不在 dialog 内跳转；Standalone 中选择相同记录继续导航到 `/agent/:agentKey?chatId=...`。
5. 根页面 Debug Panel 的 `chat.start` 行应提供 Agent、Copilot、Overview、Debug、Terminal 入口；事件或对应 chat 含合法 `shareId` 时再提供 `/share/{shareId}` 入口。不得用普通 `chatId` 拼接公开分享 URL。
6. Standalone Terminal 覆盖 open/status/write/resize；关闭 Surface 或切换 route 只 detach，关闭 tab 才 close。Desktop `/terminal` 显示 Frame Port unsupported，网络面板不得出现 guest `/ws`。
7. 连续切换 50 次路由，listener 回到基线；长流观察 pending/observer/timer 不持续增长；StrictMode 不产生重复 inbound/push 注册。
8. 设置 `DESKTOP_APP=true`：Frame Port 缺失时看到 `DESKTOP_BRIDGE_UNAVAILABLE`，transport version 错误时看到 `DESKTOP_BRIDGE_INCOMPATIBLE`。新聊天 query 不含预造 runId，只有关联 stream identity 后 URL 才获得稳定 chatId；`chat.created` 只更新列表。guest `/ws` 为 0，Main 保持单物理 Platform WS。
9. Desktop 执行 `sleep 120`：100～105 秒不出现 `WS_DISCONNECTED`、第二次 logical open 或新逻辑 Session；120 秒后 `run.finished` 直接进入原 Chat，无需切换页面。重复多次并持续 30 分钟，不能出现重复 Run、丢终态或待处理 Promise 泄漏。
10. Desktop 在已收到 Run acceptance 后强制断开物理 WS：状态进入 reconnecting，原 stream 不报错；恢复后只从 `lastSeq` 连续显示，不能重新 query。重连期间新的一次性请求返回 `PLATFORM_CONNECTION_UNAVAILABLE`；永久 Frame Port close 时待处理操作统一返回 `DESKTOP_FRAME_PORT_CLOSED`。
11. Standalone 检查真实 `/ws` 首帧必须是 connected v2，协商存活参数合法；持续业务帧可以维持连接，真实静默超过阈值才出现 `WS_DISCONNECTED`。错误版本、字段缺失和越界存活参数返回 `PLATFORM_WS_PROTOCOL_MISMATCH`，不继续运行。

12. 在 Standalone 和 Desktop WorkPanel Overview 中分别点击文件修改行：首次点击在当前 Overview 内展开 original/current 差异并显示加载或错误状态，再次点击收起，加载成功后重复展开复用缓存；两种环境均不得新建 File Diff tab。

13. 从 Main Chat 打开 Overview、Debug、BTW、Source、Planning、Artifact、Reference、File、File Diff 与 Skill，确认使用各自判别式 context 和 canonical WorkPanel descriptor；Skill context 只包含非空 `key`，重复打开只激活已有标签；Artifact/Reference 的 `module` 与 context 保持各自契约，但 route 都落到 Resource。Desktop 缺少 `currentWorker.workspaceDir` 时点击 POSIX、Windows 盘符或 UNC File 路径仍立即创建 File WorkPanel；分别使用包含和不包含 `Project/project` 的路径，File 都必须通过 management Frame Port 发送 `/api/file`，真正的 Project 页仍保持 Project 身份。文件内容正常显示；请求 `/etc/hosts` 时面板保持打开并原样展示 Platform 403，Frame Port 断开时不回退 HTTP、不显示 guest 第二条物理 `/ws`。Realtime Inspector 中 File 只有普通 request/response，不产生 query、attach 或 detach。除 Project 指定 Run/Diff 外 descriptor/URL 不含 `runId`。Web item 继续直接承载目标 HTTP(S) URL，Standalone Web 则使用全局 `/web-viewer` 且不含 Agent/Chat/Run。Overview/Debug 在 Desktop 中仅发送由宿主本地拦截的虚拟 attach，隐藏后重新激活时刷新 `/api/chat`；跨 Chat projection 隔离正确。构造本地 replay 淘汰后再以旧游标打开 Overview，确认首次 `seq_expired` 自动重新加载快照并使用新游标订阅；若新快照仍过期，只显示一次稳定错误和“重试”按钮，不循环刷新、不新增上游 attach，手动重试后才开始新的恢复周期。

11. Platform 连续发送 N 条 delta，原生 WebSocket 和 Desktop socket factory 都投影 N 个同序 event。切换 Main/Copilot/Kanban 时只 detach 一次，detach frame 先于新 query/attach；返回后从 lastSeq attach。四类本地拒绝和 Platform 4xx 均生成等价 `ApiError`。

12. 注入一个 Router 内组件渲染异常和一个 Router 外应用装配异常，均应显示 WebClient 错误页、错误摘要、技术详情与恢复按钮；不得出现空白页、无限 loading 或只有控制台错误。

Desktop 宿主回归时，确认 guest storage、URL 和 DevTools 都不出现 access token。服务冷启动直接进入 `/agent/:agentKey` 时，首个数据请求允许为 WS `/api/agent`，不依赖 `/api/agents` 预热，页面不得出现 `Failed to load agent`，且 Inspector 中只有一个有效 logical socket；`/api/agents`、`/api/agent` 与后续 `/api/chat` 均通过 Main Broker 的 Platform Socket request/response frame 完成。人为延迟 surface 登记时应在 Desktop 规定窗口内恢复；永久缺失时不得转发到 Platform。注入握手前 `1008 + surface_unavailable` 等带 reason 的 close，错误/debug 信息应包含 reason 而不是缺少 access token；显式 Retry 可重新请求，且不得自动重试或回退 HTTP。真正要求 token 的 Standalone Platform 模式在 token 为空时仍显示缺少凭据。WS 401 页面显示重新登录提示，网络异常显示网络提示且不得回退 HTTP。`/ws` 的 HTTP/Upgrade 以及 query、attach、submit、interrupt、steer、access-level HTTP 请求均应被 host 固定拒绝且不触达 Platform。用 Desktop 的 New Chat 发送首条消息后，应从 `?newChat=` replace 为稳定 `?chatId=`，并由 Desktop 外层路由选中该对话；query 失败、取消或没有稳定 ID 时不得改变当前选中项。

发布验收必须先校验 Frame Port contract hash，再渲染 Program manifest 并确认无 `/auth`、`/ws`、SSE 特例，且 `/api` 为带 `agent-platform-access-token` 的 HTTP-only route。Frame Port Desktop、匹配 WebClient 和旧任一侧混用都必须明确阻断；Standalone `localhost:11948` 仍保留自己的 `/ws`。

Desktop 右键回归覆盖用户/助手消息、代码、Web 链接、Workspace 文件、Markdown 资源与 AttachmentCard：无选区复制所属原文，有选区只复制选区；链接复用右侧 Web Preview，文件复用 Workspace Preview，资源复用鉴权预览/下载。流式更新、虚拟列表卸载和坐标漂移后执行旧命令必须无操作。抓取 resolve 响应确认不含正文、代码、路径、Token、Bearer、资源临时 URL 或菜单文案。普通浏览器右键事件不得被取消。

## 公开对话分享回归

1. 设置 `SHARE_API_BASE_URL` 指向 Tunnel，直接打开 `/share/share_xxx` 并刷新，确认始终命中 `/share/index.html`，脚本和样式从 `/share/` 加载。
2. 确认页面只请求同源 `/api/public/shares/share_xxx`，请求不携带 Cookie、Bearer 或 Desktop Token，且不会建立 `/ws`、SSE 或 Token Bridge。
3. 正常快照按 `entries` 混合 user、多个 reasoning 与 assistant；确认每轮回复聚合为一个默认折叠入口，提供重复的 `durationMs=174000` 时只显示一次“已完成 2m54s”。打开外层后，最后一条 assistant 内容之前的 reasoning snapshot 与 assistant 过程内容必须保持原始穿插顺序；每个 reasoning 以自身 `label` 或“思考过程”形成独立的第二层折叠，默认收起且互不影响，打开后显示淡色正文和引导线；仅最后一条 assistant 内容作为最终答复始终显示在外层。公开快照不接受或展示工具调用 entry。未提供 `durationMs` 时外层入口显示“已完成”。同时确认用户气泡右对齐、助手正文左对齐，顶部仅保留 AI 内容提示、不显示“更新于”。再验证 404/410、接口失败和非法 schema；确认显示稳定的只读状态，不跳转登录。
4. 使用含 GFM 表格、KaTeX、普通代码块、ECharts、Mermaid、原始 HTML、脚本 URL、图片和本地资源引用的内容验证：正文渲染与主对话一致，首次块级代码加载期间源码可见，加载后 AntD 折叠、复制、ECharts 和 Mermaid 能力完整；ECharts 只接受 JSON 对象，JavaScript 表达式和原始 HTML 不执行，危险链接不可点击，图片不发起外部或本地资源请求。
5. 在亮色、暗色、窄屏和无剪贴板权限环境验证 Header、展示及复制反馈；复制全文包含 reasoning，页面响应头包含 `X-Robots-Tag: noindex, nofollow, noarchive`。
6. 设置一个实际可访问的 HTTP(S) 客户端下载地址到 `SHARE_APP_DOWNLOAD_URL`，点击底部“打开 ZenMind”并确认浏览器发起 `zenmind://open`；页面保持可见约 1.5 秒后出现下载入口。非法或缺失 URL 时不显示下载入口，关闭悬浮条后当前页面不再展示。
7. 执行生产构建和 `npm run check:share-bundle`，确认全部分享 chunk 不包含 `src/app`、`src/features`、鉴权或 Desktop 数据模块，并仍包含共享 Markdown、reasoning disclosure、代码、ECharts 和 Mermaid 渲染器。

Agent Copilot 从 `/copilot/zenmi?lang=zh&theme=light` 发送首条消息后，WebView URL 应原位增加稳定 `chatId` 并保留宿主参数，不得触发历史 reload 或第二个 query/attach。选择历史 chat 时 URL 与加载的 ID 同步且只加载一次；点击新对话时旧 `chatId` 立即清除，首条消息后再 replace 为新 ID；切换 Agent 时清除旧对话身份并保留语言、主题参数。

Desktop 以 `wsSource=desktop-copilot` 嵌入 `/copilot` 时，顶栏右侧操作组应向左预留宿主关闭按钮空间；Desktop 原生关闭按钮不得遮挡新对话、历史或设置入口。普通浏览器独立打开 `/copilot` 时不应出现额外右侧留白。

## Markdown 图片与 Artifact 资源回归

1. 直接生成一张图片，确认回答 Markdown 只使用 `filename`，页面以带鉴权 fetch 请求 `/api/resource?file=<chatId>/<filename>` 后显示；Markdown 和事件中不得出现真实 endpoint、`file://`、当前 chatId 前缀或 `chat_dir`。
2. 生成后再发布，确认 Artifact 卡片、右侧预览、下载链接与最终回答全部使用 `artifacts/runId/filename` 发布副本，不再使用生成源 URL。
3. 用中文、空格、`#`、`%` 文件名验证 query 双层编码正确，预览字节与 MIME 正确；刷新聊天与归档后回放仍可访问。
4. 回放一条历史 `/api/resource?file=...` Markdown，确认界面显示资源不可用且不发起 fetch。外部 HTTP(S)、`data:`、`blob:` 图片仍直接加载，不转换为本地资源。
5. 分别用 Bearer 和 Cookie 登录验证 ChatScope、普通 Agent Workspace 绝对路径与 `/tmp` 的图片、PDF、HTML、文本、音视频预览和下载；错误 chatId、跨用户、Workspace 外路径及 traversal 必须由 Platform 拒绝。Team Chat 的绝对路径请求也必须发送到 Platform，由 Platform 拒绝并在面板内展示错误，前端不得按聊天类型提前短路。
6. 在回答中输出 `[灯下.md](artifacts/msx9nzkm/%E7%81%AF%E4%B8%8B.md)`、`[报告](docs/report.md)` 和 `[文本](report.txt)`；左键必须打开 Resource Viewer，不得触发下载，中文名称正确解码。绝对 Workspace 文件仍打开 File Viewer，普通 HTTP(S) 链接仍打开 Web Viewer。
7. 分别从 Artifact、Reference、普通附件和 Markdown 文件链接打开图片、PDF、HTML、文本、音频与视频：Standalone 先打开 Sidebar，Desktop 先打开 Resource WorkPanel，随后 Content Viewer 才请求 `/api/resource`。确认 Viewer 直接展示内容且不显示下载工具栏；Platform 拒绝 ChatScope、Team Chat、canonical path、symlink 或越界访问时在已打开面板内显示错误。Desktop 外层 Artifact tab 右键显示“下载产物文件”，Reference tab 显示“下载资源文件”，两者都通过当前 Resource Viewer 的统一下载执行器携带鉴权并保留正确文件名；在其他 WebClient tab 或 Viewer 卸载后发送同一动作必须无操作。
8. 用 ZIP、未知后缀、DOCX、XLSX 和 PPTX 验证不可预览路径：点击仍打开 Viewer，页面不请求资源正文、不显示伪文本、“回退下载”提示或内嵌按钮；Viewer 打开失败或预览失败均不得自动下载。

## 管理页面与弹窗布局回归

1. 在普通浏览器和 Desktop WebView 中分别打开 `/agents`、`/registries`、`/automations`、`/mcp-servers`，确认控制台从顶部铺满到底部，页面外层不出现空白或第二条滚动条；列表和详情区域各自滚动。
2. 在亮色、暗色以及宽度不超过 860px 的窗口重复检查，确认单列响应式布局仍占满可视高度。
3. 打开包含大量对话的 History Modal 和 Drawer，确认内容不超过 `70vh`，搜索工具栏保持可见，只有历史列表内部滚动。

## Agent 新建与 ZIP 导入回归

1. 打开 `/agents`，分别点击工具栏加号和空列表“创建智能体”，确认打开同一个双页签弹窗，默认页签为“ZIP 包导入”，“直接新增”位于第二项；ZIP 页签不得出现 Key 输入框，并说明 Key 来自 `agent.yml` / `agent.yaml`。
2. 切换“直接新增”并确认，弹窗应关闭且进入现有结构化创建表单；有未保存 Agent 修改时先出现放弃确认，取消后原选中项与草稿保持不变。
3. 在 ZIP 页签分别拖放和文件选择有效 ZIP，确认显示文件名、大小和“更换文件”；非 ZIP、空文件、超过 32 MiB 的文件必须在请求前拒绝。真正提交时若有未保存修改也先确认，取消后不发送上传请求且原草稿保留。
4. 导入根目录含 `agent.yml`、以及唯一包装目录含 `agent.yaml` 的包，确认 FormData 只含 `file`，成功后刷新列表和全局 Agent 缓存并选中导入 Agent；prompt、专属 Skills、`.config`、知识文件和普通资源均可在后续管理/运行中使用。
5. 导入同 Key 时，首次请求不带 `overwrite` 并收到 409；确认危险提示明确说明旧 `.config`、专属技能和资源会被整目录替换，然后同一文件以 `overwrite=true` 重试。取消覆盖应保留弹窗和已选文件，不修改旧 Agent。
6. 导入 `status: ready` 的 Agent 显示成功提示；导入因当前机器缺模型、工具、Workspace、KBASE 或 Skill 而返回 `status: invalid` 的 Agent 显示警告，仍选中并展示 diagnostics 供继续修复。
7. 缺失/重复配置文件、非法 YAML/Key/mode、路径逃逸、反斜杠、symlink、大小写/重复路径、文件目录冲突、压缩炸弹分别验证 422/413；非 ZIP 验证 415。所有失败不得切换当前 Agent，422 的 `sourcePath/message` 应在弹窗内显示。

## Skills 新建与 ZIP 导入回归

1. 打开 `/skills`，点击“新建技能”，确认弹窗默认进入“直接创建”，切换到“ZIP 包导入”后可拖放或选择一个 `.zip`，并从文件名自动填充可编辑的 Skill Key。
2. 直接创建有效 Key，确认弹窗关闭、列表新增并跳转到 `/skills/<key>`；空 Key、路径字符、`.example` 和现有 Key 必须在提交前阻止。
3. 分别导入根目录含 `SKILL.md` 和外包一层目录的 ZIP，确认成功后打开 `SKILL.md`；导入同名 Key 时弹窗保持打开并提示改名。
4. 导入非 ZIP、超过 32 MiB、缺少 `SKILL.md`、含 `../` 路径或无效 `.runtime-env.json` 的包，确认不生成技能目录，422 响应中的 `sourcePath/message` 显示在弹窗内。
5. 当前技能存在未保存文本时提交创建或导入，确认先出现放弃修改提示；取消提示后不得发送创建或导入请求。
6. 使用容器版页面重复一次接近 32 MiB 的 ZIP 导入，确认 Nginx 不返回 413，最终大小限制仍由后端统一响应。
7. 在技能文件树中选中图片文件（如 `assets/*.png`、`*.jpg`、`*.svg`），确认右侧显示图片预览，路径/大小/MIME/SHA-256 元信息仍然可见；选中视频或其他二进制文件不出现预览；切换文件或替换同路径图片后预览刷新；断网或文件被删除时显示失败文案且元信息网格保留。

## Skills 文件树操作回归

1. 选中技能后，文件树工具栏「新增文件」为弹出菜单：点击后出现「上传文件」和「新增文本文件」两项；「新增文本文件」弹出文件名输入框，并显示目标位置（根目录或当前目录），确认文件创建在锚点目录内。
2. 通过「上传文件」选择图片（png/jpg/gif/webp/svg 等），确认图片上传到当前锚点目录、文件树立即出现该项并自动选中；上传同名文件时后端应返回错误提示，不得静默覆盖。
3. 「新增目录」入口为弹出菜单，含「新建目录」与「新建子目录」两项：未选中任何项时「新建目录」在根目录创建，选中文件或目录时在其同级创建；「新建子目录」仅在选中目录时可用（未选中目录时为禁用态），在该目录内创建；创建后自动展开父目录并选中新目录。
4. 目录行主体点击后右侧显示目录信息视图（路径、子项数量与操作提示），目录可高亮选中；点击行内箭头可展开/收起，展开/收起不改变选中状态。
5. 选中目录后执行「新增文件」→ 上传或新增文本文件，确认文件创建在该目录内；「新增目录」弹窗的 placeholder 为「目录名称」（英文 Directory name），不得再出现「文件名（含扩展名）」（对应截图中的错误话术）。
6. 有未保存修改时在文件树切换选中（文件或目录），确认仍出现放弃修改确认；取消后选中不变化。
7. 对目录执行重命名/删除，确认整目录递归删除前有确认，删除后树与右侧视图同步刷新。

## Agent / Team 混合列表回归

1. 在 Platform、Gateway 与 Desktop 分别刷新左侧栏，确认 `/api/agents` 只通过主 WebSocket 发送且 payload 携带 `includeTeam=true`；`includeChats`、`includeTeam`、`scope`、`mode` 字段与 HTTP DTO 一致，断开 WS 时不得出现同接口 HTTP fallback。
2. 让上游返回交错的 Agent、Team 扁平项，其中 Team 带 `kind: "team"`、`stats` 和最近 `chats`。确认 Team 出现在左侧，最近对话可打开，未读数和总对话数优先使用 Team stats。
3. 为交错的 Agent、Team 提供不同 `updatedAt` 的最近 chat，确认默认“按时间”按最新 chat 的 `updatedAt` 倒序混排；相同时间按 `chatId` 稳定排序，无对话项保持后端混排回退顺序。切换“按名称”和新建 Agent 临时置顶仍按原交互工作。
4. 对 `scope`、`mode` 分别筛选 Agent，确认 Team 始终保留；Copilot scope 只剩 Team 时仍会按 Agent 空列表回退到 nav scope。
5. 请求 `/api/chats?mode=...`（以及 WS 对应调用），确认 Team-owned chat 没有被过滤，且从 Team 历史入口能够显示该对话。

## Agent / Team 混合列表回归

1. 在 Platform、Gateway 与 Desktop 分别刷新左侧栏，确认 `/api/agents` 只通过主 WebSocket 发送且 payload 携带 `includeTeam=true`；`includeChats`、`includeTeam`、`scope`、`mode` 字段与 HTTP DTO 一致，断开 WS 时不得出现同接口 HTTP fallback。
2. 让上游返回交错的 Agent、Team 扁平项，其中 Team 带 `kind: "team"`、`stats` 和最近 `chats`。确认 Team 出现在左侧，最近对话可打开，未读数和总对话数优先使用 Team stats。
3. 验证默认“按时间”顺序与上游按首条 `chats[0].lastRunId` 返回的混排顺序一致；切换“按名称”和新建 Agent 临时置顶仍按原交互工作。
4. 对 `scope`、`mode` 分别筛选 Agent，确认 Team 始终保留；Copilot scope 只剩 Team 时仍会按 Agent 空列表回退到 nav scope。
5. 请求 `/api/chats?mode=...`（以及 WS 对应调用），确认 Team-owned chat 没有被过滤，且从 Team 历史入口能够显示该对话。

## Side question 回归

1. 进入已有 chat，确认初始不显示 Side question Tab；执行 `/btw` 后应出现空白 Tab 并自动激活。执行 `/btw 问题` 时，首次 `/api/btw` 请求不得携带旧 `btwId`。
2. BTW idle 时确认 Composer 显示 Send；发送后必须立即切为危险态 Stop。runId/agentKey 尚未注册时 Stop 禁用，注册后启用；点击后在响应返回前显示 loading 且不能重复提交。
3. 分别在 SSE 与全局 WS 主对话模式下点击 BTW Stop，网络面板都应只出现 HTTP `POST /api/interrupt`。返回 `accepted: true` 时 BTW SSE 被本地终止并转 idle；返回 unmatched、其他 rejected 响应或模拟网络错误时仍保持 running、继续接收内容并允许再次点击 Stop。
4. BTW 回答中关闭 Tab，确认没有发送 `/api/interrupt`、当前 SSE 没有被前端 abort，Tab 立即消失并回到 Overview。等待旧流继续返回 identity、事件和 terminal 后，Tab 不得复活；再次 `/btw` 必须是空 transcript、无旧 `btwId` 的新分支，旧流不得写入新分支。
5. 刷新页面前保留一个 running BTW，刷新后确认只对持久化恢复的 run 调用一次 `/api/attach`；正常新发起的 `/api/btw` 不得并发 attach。attach not-found 不应覆盖 live run 状态或使 Stop 消失。
6. 在多个 chat 间分别创建 BTW，确认 Tab 与内容按 chat 隔离。桌面 Tab 关闭和 Copilot BTW 面板关闭都应丢弃对应 chat 的分支；右侧栏最外层关闭按钮只收起，再打开时原 BTW 仍在。
7. 独立打开 `/btw/:chatId`，先确认页面通过 `/api/chat` 解析 owner；首个 `/api/btw` 响应返回 `btwId` 后 URL 必须 replace 补全。后续多轮把该 `btwId` 作为分支上下文且可使用不同内部 runId，但 URL 不出现 runId；支持 Stop 与错误展示。刷新 `/btw/:chatId?btwId=...` 后 Timeline 必须为空，不读取 sessionStorage，下一问仍携带 URL 中的 `btwId`；Standalone BTW 不显示 Source 入口。

## 边界与非目标
- 手工测试不替代 `make test`。
- 没有可用后端时，只能验证静态页面、路由和构建，无法完成实时协议回归。
- 语音和 Desktop 桥接需要对应运行环境，普通浏览器失败不一定代表前端代码错误。

## 相关文件
- `../README.md`
- `../Makefile`
- `../webpack.config.js`
- `../nginx.conf`
- `../src/features/transport/contracts/realtimeTransport.ts`
- `../src/features/transport/lib/standaloneRealtimeTransport.ts`
- `../src/features/terminal/components/TerminalWorkspace.tsx`
- `../src/features/btw/components/BtwProvider.tsx`
- `../src/features/btw/components/BtwTab.tsx`
- `../src/features/tools/components/AwaitingHtmlContainer.tsx`
